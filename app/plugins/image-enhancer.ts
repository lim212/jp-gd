// Image Enhancer Plugin (disabled duplicate)
// This file intentionally does nothing because the active enhancer lives in plugins/image-enhancer.ts
// Keeping this stub avoids double-registration and duplicate event listeners.

import { defineNuxtPlugin } from '#app'

export default defineNuxtPlugin(() => {
  // no-op
})
