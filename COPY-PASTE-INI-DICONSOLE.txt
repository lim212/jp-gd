// 🔥 COPY SEMUA CODE INI DAN PASTE DI CONSOLE (F12)
// Lalu tekan ENTER!

(function() {
    console.clear();
    console.log('🔥 STARTING FORCE FIX...');
    
    // Cari semua div
    const allDivs = Array.from(document.querySelectorAll('div'));
    
    // 1. Cari "Butuh Bantuan"
    console.log('🔍 Mencari "Butuh Bantuan"...');
    const helpDiv = allDivs.find(div => {
        const text = div.textContent || '';
        return text.includes('Butuh Bantuan') || text.includes('Butuh bantuan');
    });
    
    if (helpDiv) {
        let wrapper = helpDiv;
        for (let i = 0; i < 3; i++) {
            if (wrapper.parentElement) wrapper = wrapper.parentElement;
        }
        wrapper.style.cssText = `
            margin-top: 100px !important;
            margin-bottom: 100px !important;
            padding: 50px !important;
            border: 10px solid red !important;
            background-color: yellow !important;
            box-shadow: 0 0 50px red !important;
            display: block !important;
        `;
        console.log('%c✅ KOTAK 1 FIXED - HARUS KUNING!', 'color: yellow; background: red; font-size: 20px; font-weight: bold; padding: 5px;');
    } else {
        console.log('%c❌ Kotak 1 NOT FOUND!', 'color: red; font-size: 16px; font-weight: bold;');
    }
    
    // 2. Cari "Trusted Partners"
    console.log('🔍 Mencari "Trusted Partners"...');
    const trustedDiv = allDivs.find(div => {
        const text = div.textContent || '';
        return text.includes('Trusted Partners');
    });
    
    if (trustedDiv) {
        let wrapper = trustedDiv;
        for (let i = 0; i < 2; i++) {
            if (wrapper.parentElement) wrapper = wrapper.parentElement;
        }
        wrapper.style.cssText = `
            margin-top: 100px !important;
            margin-bottom: 100px !important;
            padding: 50px !important;
            border: 10px solid blue !important;
            background-color: lightblue !important;
            box-shadow: 0 0 50px blue !important;
            display: block !important;
        `;
        console.log('%c✅ KOTAK 2 FIXED - HARUS BIRU MUDA!', 'color: white; background: blue; font-size: 20px; font-weight: bold; padding: 5px;');
    } else {
        console.log('%c❌ Kotak 2 NOT FOUND!', 'color: red; font-size: 16px; font-weight: bold;');
    }
    
    // 3. Cari "Jasa PayPal"
    console.log('🔍 Mencari "Jasa PayPal"...');
    const aboutDiv = allDivs.find(div => {
        const text = div.textContent || '';
        return text.includes('JASA PAYPAL TERPERCAYA') || text.includes('Apa itu Jasa PayPal');
    });
    
    if (aboutDiv) {
        let wrapper = aboutDiv;
        for (let i = 0; i < 2; i++) {
            if (wrapper.parentElement) wrapper = wrapper.parentElement;
        }
        wrapper.style.cssText = `
            margin-top: 100px !important;
            margin-bottom: 100px !important;
            padding: 50px !important;
            border: 10px solid green !important;
            background-color: lightgreen !important;
            box-shadow: 0 0 50px green !important;
            display: block !important;
        `;
        console.log('%c✅ KOTAK 3 FIXED - HARUS HIJAU MUDA!', 'color: white; background: green; font-size: 20px; font-weight: bold; padding: 5px;');
    } else {
        console.log('%c❌ Kotak 3 NOT FOUND!', 'color: red; font-size: 16px; font-weight: bold;');
    }
    
    console.log('');
    console.log('%c🎉 SELESAI!', 'color: white; background: purple; font-size: 24px; font-weight: bold; padding: 10px;');
    console.log('');
    console.log('%cSCROLL HALAMAN DAN LIHAT:', 'font-size: 18px; font-weight: bold;');
    console.log('🟡 Kotak 1 = KUNING dengan border MERAH tebal (10px)');
    console.log('🔵 Kotak 2 = BIRU MUDA dengan border BIRU tebal (10px)');
    console.log('🟢 Kotak 3 = HIJAU MUDA dengan border HIJAU tebal (10px)');
    console.log('📏 Jarak antar kotak = 100-200px!');
    console.log('');
    console.log('%cKALAU TIDAK LIHAT WARNA = Element tidak ditemukan!', 'color: red; font-size: 16px;');
})();

