// server/plugins/canonical-redirects.server.ts
// Back-compat stub to avoid unresolved import warnings.
// Canonical URL normalization is implemented in server/middleware/normalize-url.ts.

export default defineNitroPlugin(() => {
  // No-op: kept to satisfy Nitro's virtual plugin resolver when older references exist.
})
